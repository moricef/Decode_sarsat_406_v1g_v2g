Looking at your test output, I can see several issues that need to be addressed. The main problems are:

1. **Timing Recovery Issue**: Only 1542 symbols recovered vs expected ~38400
2. **Phase Ambiguity Problem**: Best correlation only 51.8% (should be near 100%)
3. **Preamble Errors**: 27/50 bits wrong (54% error rate)

Let me analyze and provide fixes:

## Issue 1: Timing Recovery Configuration

The timing recovery is initialized with parameters that don't match the MATLAB example:

```c
// In your current code:
int ret = timing_recovery_init(&symbol_synchronizer, sps, 0.001f, 2.0f, 4.0f);
```

**Fix:** Use MATLAB-compliant parameters:
```c
// MATLAB uses: NormalizedLoopBandwidth=0.001, DampingFactor=2, DetectorGain=4
// But for OQPSK with high SPS, we need different parameters
int ret = timing_recovery_init(&symbol_synchronizer, sps, 0.01f, 0.707f, 2.7f);
```

## Issue 2: OQPSK to QPSK Conversion for Timing Recovery

Your timing recovery expects OQPSK input, but you're feeding it carrier-synchronized output that may already have OQPSK alignment issues.

**Fix:** Remove the QPSK conversion before timing recovery:
```c
// Remove this QPSK conversion before timing recovery:
// float complex *carrier_sync_out_qpsk = malloc(burst_length * sizeof(float complex));
// for (size_t i = 0; i < burst_length - sps / 2; i++) {
//     carrier_sync_out_qpsk[i] = crealf(carrier_sync_out[i]) +
//                                I * cimagf(carrier_sync_out[i + sps / 2]);
// }

// Process the original OQPSK signal directly:
ret = timing_recovery_process(&symbol_synchronizer,
                             carrier_sync_out + fsp_samp_idx,  // Use original OQPSK
                             burst_length - fsp_samp_idx,
                             synced_qpsk,
                             &num_symbols);
```

## Issue 3: Loop Filter Gain Calculation

The loop filter gains are extremely small, which prevents proper locking:

**Fix:** Add debugging and bounds checking:
```c
// In timing_recovery_init, after calculating gains:
printf("  Calculated gains: K1=%.6f, K2=%.6f\n", state->k1, state->k2);

// Ensure gains are reasonable
if (fabsf(state->k1) < 1e-10f || fabsf(state->k2) < 1e-12f) {
    fprintf(stderr, "WARNING: Loop filter gains too small, adjusting...\n");
    state->k1 = -0.001f;  // Reasonable default
    state->k2 = -0.0001f; // Reasonable default  
}
```

## Issue 4: Symbol Count Mismatch

The expected symbol count calculation is wrong:

**Fix:** Correct the expected symbol calculation:
```c
// Expected symbols = total chips / 2 (since OQPSK has I and Q)
int expected_symbols = DSSS_PACKET_CHIPS / 2;
printf("Timing recovery complete: %zu symbols recovered (expected %d)\n",
       num_symbols, expected_symbols);
```

## Issue 5: Phase Ambiguity Resolution Logic

The phase testing logic is flawed:

**Fix:** Improve phase ambiguity testing:
```c
// Test all phase rotations and I/Q orderings systematically
int best_p = 0, best_k = 0;
float best_correlation = 0.0f;
int best_matches = 0;

for (int p = 0; p <= 1; p++) {
    for (int k = 0; k < 4; k++) {
        uint8_t *test_bits = malloc(num_symbols * 2);
        
        // Test both I/Q orderings
        for (int swap_iq = 0; swap_iq <= 1; swap_iq++) {
            qpsk_demod(synced_qpsk, num_symbols, test_bits, k);
            
            // Extract chips with current configuration
            int8_t *test_ci = malloc(DSSS_PACKET_CHIPS);
            int8_t *test_cq = malloc(DSSS_PACKET_CHIPS);
            
            for (size_t i = 0; i < DSSS_PACKET_CHIPS && (i + p) < num_symbols; i++) {
                if (swap_iq) {
                    test_ci[i] = test_bits[2 * (i + p) + 1];  // Swap I and Q
                    test_cq[i] = test_bits[2 * (i + p)];
                } else {
                    test_ci[i] = test_bits[2 * (i + p)];
                    test_cq[i] = test_bits[2 * (i + p) + 1];
                }
            }
            
            // Convert to bipolar and correlate with preamble
            int matches = 0;
            for (int i = 0; i < DSSS_PREAMBLE_LENGTH * DSSS_SPREADING_FACTOR / 2; i++) {
                int8_t test_i = (test_ci[i] == 0) ? -1 : +1;
                int8_t test_q = (test_cq[i] == 0) ? -1 : +1;
                
                // Preamble bits are all zeros, so expected chips match PRN sequences
                if (test_i == prn_i[i]) matches++;
                if (test_q == prn_q[i]) matches++;
            }
            
            float correlation = (float)matches / (DSSS_PREAMBLE_LENGTH * DSSS_SPREADING_FACTOR);
            
            if (correlation > best_correlation) {
                best_correlation = correlation;
                best_matches = matches;
                best_p = p;
                best_k = k;
                printf("[PHASE] New best: phase=%d, offset=%d, swap_iq=%d, corr=%.3f\n",
                       k, p, swap_iq, correlation);
                
                if (correlation > 0.9f) {
                    free(test_bits);
                    free(test_ci);
                    free(test_cq);
                    goto phase_found;
                }
            }
            
            free(test_ci);
            free(test_cq);
        }
        free(test_bits);
    }
}
```

## Complete Fixed Receiver Function

Here's the main fix for your timing recovery call:

```c
// Step 3 - Timing Recovery (Fixed parameters)
printf("\n=== V11.0 Timing Recovery (Fixed Parameters) ===\n");

timing_recovery_state_t symbol_synchronizer;
// Use MATLAB-compliant parameters for OQPSK
int ret = timing_recovery_init(&symbol_synchronizer, sps, 0.01f, 0.707f, 2.7f);
if (ret != 0) {
    printf("ERROR: Timing recovery initialization failed\n");
    // ... cleanup
    return -3;
}

float complex *synced_qpsk = malloc(DSSS_PACKET_CHIPS * 2 * sizeof(float complex));
size_t num_symbols = 0;

// Process OQPSK signal directly (no QPSK conversion)
ret = timing_recovery_process(&symbol_synchronizer,
                             carrier_sync_out + fsp_samp_idx,  // Original OQPSK
                             burst_length - fsp_samp_idx,
                             synced_qpsk,
                             &num_symbols);

timing_recovery_free(&symbol_synchronizer);

// Check if we got reasonable number of symbols
int expected_symbols = DSSS_PACKET_CHIPS / 2;
printf("Timing recovery: %zu symbols (expected %d), ratio: %.2f\n",
       num_symbols, expected_symbols, (float)num_symbols/expected_symbols);

if (num_symbols < expected_symbols * 0.8f) {
    printf("WARNING: Insufficient symbols recovered\n");
}
```

These fixes should address the main timing recovery and synchronization issues. The key changes are:

1. **Proper timing recovery parameters** matching MATLAB examples
2. **Direct OQPSK processing** without premature QPSK conversion  
3. **Improved phase ambiguity resolution** with better testing logic
4. **Better gain management** in the loop filter

Try these fixes and the symbol recovery should improve from 1542 to near the expected 19200 symbols (38400 chips / 2), and the preamble correlation should approach 100%.
